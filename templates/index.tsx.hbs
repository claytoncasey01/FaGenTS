{{#each iconComponents}}
import {{this}} from "./{{this}}";
{{/each}}

export {
  {{#each iconComponents}}
  {{#unless @last}}
  {{this}},
  {{/unless}}
  {{#if @last}}
  {{this}}
  {{/if}}
  {{/each}}
};
